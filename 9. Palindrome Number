class Solution {
    public boolean isPalindrome(int x) {
        String reverse_x = new StringBuilder(String.valueOf(x)).reverse().toString();
        return reverse_x.equals(String.valueOf(x));
    }
}
